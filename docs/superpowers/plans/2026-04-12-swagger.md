# Swagger Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为项目增加 Swagger UI 和按用户权限裁剪的动态 OpenAPI 文档。

**Architecture:** 在 `internal/app` 内新增 OpenAPI 元数据与构建逻辑，通过已有 JWT claims 判断当前用户权限；Swagger UI 指向动态 `/openapi.json`。

**Tech Stack:** Fiber v3, `github.com/gofiber/contrib/v3/swaggo`, Go tests

---

### Task 1: 定义 OpenAPI 元数据与过滤逻辑

**Files:**
- Create: `internal/app/openapi.go`
- Test: `internal/app/openapi_test.go`

- [ ] 写过滤逻辑测试
- [ ] 运行测试确认失败
- [ ] 实现最小 OpenAPI 构建与权限过滤
- [ ] 运行测试确认通过

### Task 2: 接入 Swagger 路由

**Files:**
- Modify: `internal/app/app.go`
- Create: `internal/app/openapi.go`
- Test: `internal/app/openapi_test.go`

- [ ] 写系统路由测试
- [ ] 运行测试确认失败
- [ ] 注册 `/openapi.json` 与 `/swagger/*`
- [ ] 运行测试确认通过

### Task 3: 文档与依赖

**Files:**
- Modify: `go.mod`
- Modify: `README.md`

- [ ] 增加 Swagger 依赖
- [ ] 更新 README 使用说明
- [ ] 运行定向测试与构建验证
