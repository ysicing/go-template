version: '3'

vars:
  BINARY: id
  VERSION:
    sh: git describe --tags --always --dirty 2>/dev/null || echo dev
  GIT_COMMIT:
    sh: git rev-parse --short HEAD 2>/dev/null || echo unknown
  BUILD_DATE:
    sh: date -u '+%Y-%m-%dT%H:%M:%SZ'
  LDFLAGS: -s -w -X main.Version={{.VERSION}} -X main.GitCommit={{.GIT_COMMIT}} -X main.BuildDate={{.BUILD_DATE}}

tasks:
  default:
    cmds:
      - task: build

  fmt:
    desc: Format code
    cmds:
      - gofmt -s -w .
      - goimports -w .
      - gci write --skip-generated --custom-order -s standard -s default -s "prefix(github.com/ysicing/go-template)" -s blank -s dot -s alias .
      - cd web && pnpm run lint --fix

  web:
    desc: Build frontend
    cmds:
      - cd web && pnpm install && pnpm run build

  build:
    desc: Build backend
    deps: [web]
    cmds:
      - CGO_ENABLED=0 go build -ldflags="{{.LDFLAGS}}" -o {{.BINARY}} .

  image:
    desc: Build Docker image
    cmds:
      - docker build -t {{.BINARY}} .

  clean:
    desc: Clean build artifacts
    cmds:
      - rm -f {{.BINARY}}
      - rm -rf web/dist web/node_modules

  run:
    desc: Run backend (dev)
    env:
      SECURITY_ALLOW_INSECURE: '{{default "true" .SECURITY_ALLOW_INSECURE}}'
    cmds:
      - go run .

  dev:
    desc: Run frontend dev server
    dir: web
    cmds:
      - pnpm run dev
